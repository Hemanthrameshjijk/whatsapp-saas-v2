server:
  port: 8080
  tomcat:
    threads:
      max: 200

spring:
  threads:
    virtual:
      enabled: true
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/whatsappai}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:root}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    repair-on-migrate: true
    validate-on-migrate: false
    out-of-order: true
    ignore-migration-patterns: "*:missing"
  data:
    redis:
      host: "${REDIS_HOST:localhost}"
      port: ${REDIS_PORT:6379}
      repositories:
        enabled: false

ai:
  active-model: "llama3.2:1b"
  embed-model: "nomic-embed-text"
  temperature: 0.4
  max-tokens: 256
  timeout-ms: 60000
  ollama:
    base-url: "${OLLAMA_URL:http://localhost:11434}"

qdrant:
  host: "${QDRANT_HOST:localhost}"
  port: ${QDRANT_PORT:6334}
  use-tls: false

redis:
  session-ttl-minutes: 30
  business-config-ttl-minutes: 10

whatsapp:
  verify-token: "${WA_VERIFY_TOKEN:your_custom_verify_token_here}"
  api-url: "https://graph.facebook.com/v18.0"
  phone-number-id: "${WA_PHONE_NUMBER_ID:1039904349204231}"
  access-token: "${WA_ACCESS_TOKEN:}"
  app-secret: "${WA_APP_SECRET:719ddeb624be64338ae6b0bcdf078a35}"

jwt:
  secret: "${JWT_SECRET:changethisinproductionmustbe32chars!}"
  expiry-hours: 24

geocode:
  provider: ${GEOCODE_PROVIDER:nominatim}
  nominatim-url: https://nominatim.openstreetmap.org

logging:
  level:
    com.whatsappai: DEBUG
    org.springframework.security: WARN
